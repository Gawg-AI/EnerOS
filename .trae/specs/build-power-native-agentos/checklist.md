- [x] AuthorityLevel 枚举定义完整，包含 4 个等级及权限规则
- [x] SystemOperatingState 枚举定义完整，包含 5 个状态及转换规则
- [x] InterlockingRule 结构体定义完整
- [x] AuditEntry 结构体定义完整，包含所有审计字段
- [x] ActionVerdict 枚举定义完整，包含 4 种判定结果
- [x] Jurisdiction 结构体定义完整
- [x] EnerOSConfig 新增 AgentConfig 和 EmergencyConfig
- [x] Agent trait 新增 authority_level() / jurisdiction() / handle_emergency() 方法
- [x] AgentAction 新增 RequestApproval / DelegateTask / EmergencyOverride / RollbackAction 变体
- [x] AgentContext 新增 constraint_engine / system_state / authority / audit_trail 引用
- [x] ConstraintAwareValidator 实现完整的验证管线（权限→管辖→约束预校核→闭锁→审批）
- [x] ConstraintEngine 新增 check_action_feasibility() 方法
- [x] ConstraintEngine 新增动态阈值调整和违规主动通知
- [x] InterlockingRuleEngine 实现至少 3 条内置闭锁规则
- [x] ActionConflictResolver 实现冲突检测和基于权限/拓扑的仲裁
- [x] SystemOperatingState 状态机实现完整的状态转换和自动动作
- [x] EmergencyResponsePipeline 实现至少 3 个内置紧急响应方案
- [x] TopologyAwareScheduler 实现基于管辖区域的事件路由
- [x] AuditTrail 实现 append-only 日志和完整性校验
- [x] ActionDispatcher 集成 ConstraintAwareValidator + InterlockingRuleEngine + ActionConflictResolver + AuditTrail
- [x] AgentOrchestrator 集成 SystemOperatingState + TopologyAwareScheduler + EmergencyResponsePipeline
- [x] DispatchResult 新增 Rejected / ConstraintRejected / PendingApproval / ConflictDetected / EmergencyBypassed 变体
- [x] cargo test --workspace 全部通过
- [x] cargo clippy --workspace 无错误
