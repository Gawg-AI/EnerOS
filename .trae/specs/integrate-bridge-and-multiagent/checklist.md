- [x] Python HTTP Bridge 服务启动并可响应健康检查
- [x] Rust BridgeClient 可启动/调用/关闭 Python HTTP 服务
- [x] pandapower 潮流结果（电压/功率/损耗/收敛）可回传 Rust 端
- [x] build_full_network 返回完整拓扑，Rust 端可构造 PowerNetwork 并求解潮流
- [x] load_all_loads() 使用正确数据源，不再调用 list_validation_rules
- [x] 开关柜/无功补偿/新能源设备有 Rust 端解析方法
- [x] Agent 可通过 AgentContext.send_message() 向其他 Agent 发送消息
- [x] 协作协议支持 Coordinator→Executor→Advisor 角色分配与反馈
- [x] 端到端协作测试通过：约束违规 → 分配 → 执行 → 反馈
- [x] cargo test --workspace 全部通过
- [x] cargo clippy --workspace 无错误
