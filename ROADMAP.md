# EnerOS 开发路线图

> 本文件基于代码审查结果规划未来版本，与 [CHANGELOG.md](./CHANGELOG.md) 配合使用。
> 每个规划项都标注了**代码现状依据**和**验收标准**。

---

## 当前状态（v0.2.0 审查基线）

| 指标 | 数值 |
|------|------|
| 版本 | 0.2.0 |
| Crate 数 | 19 |
| 代码行数 | ~44,000 |
| 测试数 | 930+ |
| 编译警告 | 0 |
| 测试失败 | 0 |
| TODO/FIXME | 0 |
| 空壳函数 | 4（已识别） |
| 简化实现 | 10（已识别，均有生产级替代路径） |

### 代码审查发现的问题清单

| # | 严重度 | 位置 | 问题 | 状态 |
|---|--------|------|------|------|
| P1 | 高 | `Cargo.toml` workspace.package | 版本号 `0.1.0` 与实际 `0.2.0` 不一致 | 待修复 |
| P2 | 高 | `dashboard/assets.rs` APP_JS | 前端请求 `/api/flow`、`/api/data` 等不存在的端点 | 待修复 |
| P3 | 中 | `dashboard/full_page.rs:37` | flow-panel 重复使用 topology_svg 变量 | 待修复 |
| P4 | 中 | `api/handlers/topology.rs` | TopologyEngine 可用时返回空 buses/branches | 待修复 |
| P5 | 中 | `api/handlers/analysis.rs` opf_handler | 硬编码 `p_limit_mw: 200.0`、`reactance_pu: 0.1` | 待修复 |
| P6 | 中 | `api/handlers/ws.rs` | WebSocket Text 消息处理为空（`let _ = text;`） | 待修复 |
| P7 | 中 | `api/handlers/health.rs` | 仅返回 `{"status":"ok"}`，无系统健康检查 | 待修复 |
| P8 | 低 | `device/iec104/mod.rs` | `clock_synchronization()` 为桩函数 | 待修复 |
| P9 | 低 | `device/iec61850/mod.rs` | `subscribe_dataset()` 为桩函数 | 待修复 |
| P10 | 低 | `device/mqtt.rs` | `subscribe()` 注册回调但未触发 | 待修复 |
| P11 | 低 | `device/discovery.rs` | 始终返回 `ProtocolType::Modbus` | 待修复 |
| P12 | 低 | `bridge/equipment_bridge.rs` | `load_all_loads()` 返回空 Vec | 待修复 |
| P13 | 低 | `network/simulator.rs` | 开关动作保守拒绝（拓扑物理未建模） | 待修复 |
| P14 | 低 | `constraint/engine.rs` | `check_stability` 为简化电压裕度计算 | 待修复 |
| P15 | 低 | `powerflow/matrix.rs` | YBusMatrix 使用密集矩阵，大网络性能差 | 待优化 |

---

## v0.2.1 — Bug 修复与一致性校准（立即执行）

### 目标：修复代码审查发现的 15 个问题

#### P1: 版本号同步
- [ ] `Cargo.toml` workspace.package version 改为 `0.2.0`
- **验收**: `cargo metadata` 输出版本为 0.2.0

#### P2: Dashboard 前后端 API 对齐
- [ ] 修复 `assets.rs` APP_JS 中的端点路径，匹配实际 API 路由
- **验收**: Dashboard 页面加载后浏览器控制台无 404 错误

#### P3: Dashboard flow-panel 显示修复
- [ ] `full_page.rs` flow-panel 使用独立的 flow_heatmap SVG 而非 topology_svg
- **验收**: Dashboard 页面 flow-panel 显示热力图而非拓扑图

#### P4: Topology API 返回真实数据
- [ ] `handlers/topology.rs` 从 TopologyEngine 提取 bus/branch 列表
- **验收**: `GET /api/topology` 返回非空 buses 和 branches 数组

#### P5: OPF Handler 使用真实网络参数
- [ ] `handlers/analysis.rs` opf_handler 从 PowerNetwork 提取真实 p_limit 和 reactance
- **验收**: OPF 请求使用网络实际参数而非硬编码值

#### P6: WebSocket 命令处理
- [ ] `handlers/ws.rs` 实现 Text 消息解析和命令分发
- **验收**: WebSocket 客户端可发送 JSON 命令并收到响应

#### P7: 健康检查端点增强
- [ ] `handlers/health.rs` 检查关键组件状态（DB 连接、设备连接数、Agent 运行数）
- **验收**: `GET /health` 返回各子系统健康状态

#### P8-P12: 协议适配器桩函数补全
- [ ] P8: IEC 104 `clock_synchronization()` 发送 C_CS_NA_1 命令
- [ ] P9: IEC 61850 `subscribe_dataset()` 实现报告订阅
- [ ] P10: MQTT `subscribe()` 消息到达时触发回调
- [ ] P11: `discovery.rs` 增加协议探测（端口+协议握手）
- [ ] P12: `equipment_bridge.rs` `load_all_loads()` 从 cnpower 加载负荷数据
- **验收**: 各桩函数有对应单元测试

#### P13: 网络仿真器开关拓扑建模
- [ ] `network/simulator.rs` 实现开关开合的拓扑变化 + 重新潮流计算
- **验收**: 开关动作后潮流结果反映拓扑变化

#### P14: 电压稳定分析增强
- [ ] `constraint/engine.rs` `check_stability` 实现连续潮流（CPF）或模态分析
- **验收**: 稳定裕度计算基于物理模型而非经验公式

#### P15: Y-Bus 稀疏矩阵
- [ ] `powerflow/matrix.rs` 改用稀疏矩阵存储（CSR 格式）
- **验收**: 100+ 节点网络潮流计算时间 < 100ms

---

## v0.3.0 — 生产就绪（目标：2026-07）

### 目标：从"能跑"到"能部署"

#### 1. 配置体系
- [ ] 统一 TOML 配置文件（`eneros.toml`），覆盖所有子系统参数
- [ ] 环境变量覆盖支持（`ENEROS_*` 前缀）
- [ ] 配置校验和启动时友好报错
- [ ] `main.rs` 从配置文件初始化所有组件（当前为硬编码）
- **验收**: 一份配置文件可启动完整系统

#### 2. 持久化全面接入
- [ ] `eneros-api` 启动时从配置创建 `TimeSeriesEngine::with_sqlite()`
- [ ] `eneros-gateway` 命令历史持久化到 SQLite
- [ ] `eneros-agent` Agent 记忆系统接入 SQLite 后端（当前仅文件）
- [ ] 数据库 schema 版本管理和迁移脚本
- **验收**: 重启后时序数据、命令历史、Agent 记忆不丢失

#### 3. 可观测性
- [ ] 结构化日志（JSON 格式），支持日志级别动态调整
- [ ] Prometheus metrics 端点（`/metrics`）：
  - 命令执行延迟直方图
  - 命令队列深度
  - 约束违规计数器
  - Agent 决策延迟
  - 设备连接状态
  - 潮流计算迭代次数
- [ ] 分布式追踪（trace_id 贯穿 API→Pipeline→Gateway→Device）
- **验收**: Prometheus 可抓取 metrics，Grafana 可可视化

#### 4. 安全加固
- [ ] API 认证（JWT token 或 API key）
- [ ] 命令执行权限审计日志（who/what/when/result）
- [ ] 设备连接 TLS 加密（IEC 104/T 62351、MQTT over TLS）
- [ ] 配置文件敏感信息脱敏
- **验收**: 未认证请求被拒绝，审计日志可追溯所有命令

#### 5. 错误处理统一
- [ ] 全局错误处理中间件（API 层统一错误响应格式）
- [ ] 设备断连自动重连机制
- [ ] 约束引擎异常时的安全降级策略
- **验收**: 任何单点故障不导致系统崩溃

---

## v0.4.0 — 协议覆盖完善（目标：2026-08）

### 目标：覆盖主流工业协议，达到实际部署能力

#### 1. IEC 104 增强
- [ ] 支持更多 ASDU 类型（双点信息、步位置信息、BCR 总计）
- [ ] 支持事件触发传输（Spontaneous）和周期扫描两种模式
- [ ] 支持时钟同步命令（C_CS_NA_1）—— 补全 P8
- [ ] 支持参数下装（Para_Para_Set）
- [ ] 支持双机冗余（双连接监视）
- **验收**: 与真实 RTU 互通测试通过

#### 2. IEC 61850 增强
- [ ] 实现报告控制块（RCB）使能和报告接收 —— 补全 P9
- [ ] 支持 GOOSE 订阅（Layer 2 原始套接字）
- [ ] 支持 SCL（变电站配置语言）文件解析
- [ ] 支持数据集操作（创建/删除/读取）
- [ ] 支持控制服务（Select/Operate/Cancel）
- **验收**: 与真实 IED 互通测试通过

#### 3. Modbus 增强
- [ ] 验证现有 Modbus TCP 适配器的真实 TCP 连接
- [ ] 添加 Modbus RTU（串口 RS-485）支持
- [ ] 批量读/写优化（Function Code 23）
- [ ] 支持保持寄存器、输入寄存器、线圈、离散输入全部 4 种数据类型
- **验收**: 与真实 PLC 互通测试通过

#### 4. MQTT 增强
- [ ] 消息到达时触发回调 —— 补全 P10
- [ ] 支持 QoS 0/1/2
- [ ] 支持 Last Will 和 Retained 消息
- [ ] 支持 Topic 通配符订阅
- **验收**: 与 MQTT Broker 互通测试通过

#### 5. 新增协议
- [ ] DNP3 适配器（北美市场常用）
- [ ] OPC UA 客户端适配器
- **验收**: 基本读/写功能可用

#### 6. 设备发现增强
- [ ] 协议探测（端口+协议握手）—— 补全 P11
- [ ] 支持广播发现（OPC UA Discovery Service）
- **验收**: 能自动识别网络中的设备协议类型

---

## v0.5.0 — Agent 智能化（目标：2026-09）

### 目标：从规则驱动到智能决策

#### 1. LLM 集成深化
- [ ] 支持多 LLM 后端切换（OpenAI、Anthropic、本地模型）
- [ ] Agent 推理链可追溯（每步推理记录到审计日志）
- [ ] LLM 调用成本控制和限流
- [ ] 支持流式输出（Streaming）
- [ ] 支持 Function Calling / Tool Use 标准化
- **验收**: Agent 可使用 LLM 完成复杂推理并记录完整链路

#### 2. Agent 协同增强
- [ ] 多 Agent 任务分配和负载均衡
- [ ] Agent 间消息传递和状态共享
- [ ] 分布式 Agent 部署支持
- [ ] Agent 生命周期管理增强（暂停/恢复/迁移）
- **验收**: 多个 Agent 可协同完成跨域任务

#### 3. 自愈策略库
- [ ] 电压越限自愈策略（电容器投切、变压器分接头调节、发电机无功调整）
- [ ] 线路过载自愈策略（负荷转移、发电机出力调整）
- [ ] 频率异常自愈策略（一次调频、二次调频、需求响应）
- [ ] 故障隔离和恢复策略（故障定位、隔离、非故障区供电恢复）
- [ ] 开关拓扑变化建模 —— 补全 P13
- **验收**: 每种策略有端到端测试验证

#### 4. 预测精度
- [ ] ARIMA 模型实现
- [ ] 神经网络预测（LSTM）可选集成
- [ ] 预测准确度评估和自动模型选择
- [ ] 支持多时间尺度预测（超短期/短期/中期/长期）
- **验收**: 预测精度 MAPE < 3%

#### 5. 记忆系统增强
- [ ] Agent 记忆接入 SQLite 后端
- [ ] 语义记忆检索（向量相似度）
- [ ] 记忆遗忘策略（基于时间/重要性/访问频率）
- **验收**: Agent 可从历史记忆中学习并改进决策

---

## v0.6.0 — 分析层进阶（目标：2026-10）

### 目标：工程级分析精度

#### 1. 状态估计增强
- [ ] 不良数据检测和辨识（最大标准残差法、r^N_test）
- [ ] 可观测性分析
- [ ] 变压器分接头估计
- [ ] 相量测量单元（PMU）接入和线性状态估计
- [ ] 拓扑错误辨识
- **验收**: 可检测并剔除坏数据，估计精度 RMSE < 0.01pu

#### 2. OPF 增强
- [ ] 交流 OPF（AC-OPF）完整实现
- [ ] 安全约束 OPF（SCOPF）
- [ ] 机组组合（Unit Commitment）优化
- [ ] 实时电价计算和发布
- [ ] OPF Handler 使用真实网络参数 —— 补全 P5
- **验收**: AC-OPF 与 MATPOWER 结果对比误差 < 1%

#### 3. 短路增强
- [ ] 动态短路分析（考虑发电机暂态电抗）
- [ ] 继电保护配合校验
- [ ] 故障穿越能力评估
- [ ] 多故障场景分析
- **验收**: 短路电流计算与 ETAP 结果对比误差 < 5%

#### 4. 暂态稳定
- [ ] 时域仿真（龙格-库塔法）
- [ ] 临界故障清除时间计算
- [ ] 等面积法则快速评估
- [ ] 电压稳定分析（CPF/模态分析）—— 补全 P14
- **验收**: 暂态稳定仿真与 PSSE 结果对比趋势一致

#### 5. 稀疏矩阵优化
- [ ] Y-Bus 矩阵改用 CSR 稀疏存储 —— 补全 P15
- [ ] 牛顿-拉夫逊求解器使用稀疏 LU 分解
- [ ] 支持大规模网络（1000+ 节点）
- **验收**: 1000 节点网络潮流计算 < 500ms

---

## v0.7.0 — 高可用部署（目标：2026-11）

### 目标：生产级部署能力

#### 1. 容器化
- [ ] Dockerfile（多阶段构建，优化镜像大小）
- [ ] docker-compose.yml（含 API + SQLite + 示例设备模拟器）
- [ ] 健康检查和就绪探针
- [ ] 配置文件外部化（环境变量 + volume 挂载）
- **验收**: `docker compose up` 一键启动完整系统

#### 2. 集群部署
- [ ] 主备冗余（心跳 + 自动切换）
- [ ] 数据库主从复制
- [ ] 配置中心化（etcd/Consul）
- [ ] 负载均衡（多 API 实例）
- **验收**: 主节点故障后备节点 < 3s 接管

#### 3. 灾备
- [ ] 数据库定期备份和恢复脚本
- [ ] 配置导出/导入
- [ ] 故障恢复 Runbook
- [ ] 灾难恢复演练自动化
- **验收**: 数据恢复 RTO < 5min，RPO < 1min

#### 4. 性能优化
- [ ] 热点路径性能分析（命令执行 < 10ms 目标）
- [ ] 内存使用优化（时序数据压缩存储）
- [ ] 连接池复用（设备连接、数据库连接）
- [ ] 异步任务调度优化
- **验收**: 1000 TPS 命令处理，P99 延迟 < 50ms

#### 5. CI/CD
- [ ] GitHub Actions: push 时自动运行 cargo test + clippy + fmt
- [ ] 自动构建 Docker 镜像并推送到 GHCR
- [ ] 自动发布 Release（tag 触发）
- [ ] 代码覆盖率报告（tarpaulin）
- **验收**: PR 合并前 CI 全绿

---

## v0.8.0 — 生态扩展（目标：2026-12）

### 目标：构建生态

#### 1. 插件系统
- [ ] 协议适配器插件接口（动态加载 .so/.dll）
- [ ] Agent 策略插件接口
- [ ] 分析模块插件接口
- [ ] 插件版本管理和兼容性检查
- **验收**: 第三方可开发插件并热加载

#### 2. API 网关
- [ ] GraphQL API（灵活查询）
- [ ] WebSocket 实时推送（告警、状态变化）—— 补全 P6
- [ ] API 版本管理（`/api/v1/`、`/api/v2/`）
- [ ] API 限流和配额管理
- [ ] OpenAPI/Swagger 自动文档生成
- **验收**: API 文档可交互测试

#### 3. 可视化增强
- [ ] 电网拓扑图实时渲染（基于地理位置）
- [ ] 历史数据趋势图
- [ ] 告警管理和通知（邮件、短信、Webhook）
- [ ] 3D 厂站可视化
- [ ] Dashboard 前后端 API 对齐 —— 补全 P2/P3
- **验收**: Dashboard 实时反映电网状态

#### 4. 文档体系
- [ ] API 文档（OpenAPI/Swagger）
- [ ] 开发者指南（架构、贡献指南、代码规范）
- [ ] 部署运维手册
- [ ] 架构设计文档（ADR）
- [ ] 用户手册
- **验收**: 新开发者可按文档独立部署系统

#### 5. 测试体系
- [ ] 端到端集成测试（真实设备模拟器）
- [ ] 性能基准测试（criterion）
- [ ] 混沌工程测试（设备断连、网络分区）
- [ ] 协议一致性测试套件
- **验收**: 测试覆盖率 > 80%

---

## 长期愿景（v1.0+）

### 电力市场交易
- 日前市场出清
- 实时市场平衡
- 辅助服务市场（调频、备用）
- 金融输电权（FTR）

### 数字孪生
- 实时电网镜像
- What-If 场景推演
- 历史回放和对比分析
- 虚拟传感器

### AI 驱动优化
- 强化学习自动调参
- 异常检测和根因分析
- 预测性维护
- 自主决策（L4 级自治）

### 标准合规
- IEC 61968/61970 CIM 模型
- IEC 62351 安全标准
- 国网/南网通信规约
- 电力监控系统安全防护规范

---

## 版本发布节奏

| 节奏 | 说明 |
|------|------|
| **修订版本** (0.0.X) | 按需发布，仅包含 Bug 修复 |
| **次版本** (0.X.0) | 每 4-6 周一个次版本，包含新功能 |
| **主版本** (1.0.0) | 达到生产级稳定性后发布 |

## 优先级原则

1. **P0 — 安全/数据完整性**：数据丢失、安全漏洞优先修复
2. **P1 — 核心功能**：影响主流程的 Bug 和功能缺失
3. **P2 — 体验优化**：性能、可观测性、文档
4. **P3 — 新功能扩展**：新协议、新分析模块、新 Agent 策略

## 代码审查基线

本路线图的每个规划项都基于 v0.2.0 代码审查结果。审查覆盖：
- 19 个 crate，~44,000 行代码
- 930+ 测试，0 失败
- 0 个 TODO/FIXME 标记
- 15 个已识别问题（P1-P15），按严重度分级
- 所有简化实现均有对应的生产级替代路径
