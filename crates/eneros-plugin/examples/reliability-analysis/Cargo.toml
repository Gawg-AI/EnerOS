[package]
name = "reliability-analysis"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib"]

[dependencies]
eneros-plugin = { path = "../.." }
serde = { version = "1", features = ["derive"] }
serde_json = "1"

# 声明为独立工作区，避免被父 workspace 自动包含
[workspace]
