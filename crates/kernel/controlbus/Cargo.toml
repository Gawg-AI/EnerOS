[package]
name = "eneros-controlbus"
version = "0.22.0"
edition = "2021"
authors = ["EnerOS Team"]
license = "MIT OR Apache-2.0"

[dependencies]
eneros-ipc = { path = "../ipc" }
eneros-sched = { path = "../sched" }
